# URL của Backend API
# Khi chạy local: để trống hoặc comment out (Vite proxy sẽ tự forward /api → localhost:3001)
# Khi deploy: đặt URL đầy đủ của backend
# VITE_API_URL=https://traitimviet-api.onrender.com/api
